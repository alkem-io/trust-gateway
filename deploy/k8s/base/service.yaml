apiVersion: v1
kind: Service
metadata:
  name: trust-gateway
  labels:
    app.kubernetes.io/name: trust-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trust-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
